<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/headerbackgroung"
    android:orientation="vertical">

    <com.mikhaellopez.circularimageview.CircularImageView
        android:layout_width="85dp"
        android:layout_height="85dp"
        android:id="@+id/profileimg"
        android:src="@drawable/man_navigation"
        app:civ_border_color="#FFFFFF"
        app:civ_border_width="1dp"
        android:layout_marginLeft="24dp"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="24dp"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Aapka Naam"
        android:layout_below="@+id/profileimg"
        android:textSize="20dp"

        android:layout_marginLeft="20dp"
        android:id="@+id/username"
        android:textColor="#FFF"
        android:textStyle="bold"
        android:layout_above="@+id/useremail"
        android:gravity="left"
        android:layout_alignLeft="@+id/profileimg"
        android:layout_alignStart="@+id/profileimg"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Aapka E-mail"
        android:textSize="15dp"
        android:layout_marginLeft="20dp"
        android:id="@+id/useremail"
        android:layout_below="@+id/username"
        android:textColor="#FFF"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@+id/username"
        android:layout_alignStart="@+id/username"
        />

</LinearLayout>